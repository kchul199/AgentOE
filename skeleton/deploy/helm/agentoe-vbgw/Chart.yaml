apiVersion: v2
name: agentoe-vbgw
description: |
  Agentic Callbot Voice Bridge Gateway — gRPC (50051) + WebSocket (50052) 음성 스트림 처리 서비스.
  통화당 long-lived connection 을 가지므로 PDB / preStop drain / NLB(L4) 친화 패턴.
type: application
version: 0.1.0
appVersion: "1.0.0"
kubeVersion: ">=1.28.0-0"
keywords:
  - agentoe
  - vbgw
  - voice
  - grpc
  - websocket
maintainers:
  - name: agentoe-platform
    email: platform@agentoe.io
